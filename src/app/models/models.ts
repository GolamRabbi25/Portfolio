export interface Education {
    course: string;
    institute: string;
    duration: string;
    score: string;
}
export interface Skill {
    name: string;
    level: string;
    rating: string;
}
export interface Project {
    tittle: string;
    technologies: string;
    description: string[];
}